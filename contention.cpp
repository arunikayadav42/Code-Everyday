#include <bits/stdc++.h>

using namespace std;

int main()
{
	int t;
	cin>>t;
	int temp = 1;
	while(temp != t+1)
    {
    	int n, q;
    	cin>>r>>c;
    	vector<pair<int, int>> vec;
        for(int i = 0; i < q; i++)
        {
        	int li, ri;
        	cin>>li>>ri;
        	vec[i].push_back({li, ri});
        }
    }

	return 0;
}