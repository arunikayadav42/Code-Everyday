#include <bits/stdc++.h>
#define ll long long int

using namespace std;

int main()
{

    int n, m, k;
    cin>>n>>m>>k;
    vector<pair<int, int>> vec(k);
    ll length_of_tape = n;
    for(int i = 0; i < m; i++)
    {
    	cin>>vec[i].first;
    	vec[i].second = 1;
    }
    int total_groups = n;
    int i = 2;
    while(i <= n)
    {
       
    }
	return 0;
}